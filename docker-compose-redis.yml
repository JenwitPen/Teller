version: '3.8'
services:
  redis:
    image: redis:alpine
    container_name: local-redis
    ports:
      - "6379:6379"
    volumes:
      - redis-data:/data
    command: redis-server --appendonly yes # เปิดโหมดเซฟข้อมูลลงดิสก์

volumes:
  redis-data: